# OmniToolaz AI Life Planner

AI productivity planner using Firebase auth + Cloudflare Worker AI.

## Setup
1. Create Firebase project
2. Enable Email + Google login
3. Paste config in firebase.js
4. Deploy on GitHub Pages

Next: Connect Cloudflare Worker AI.
git remote add origin https://github.com/SUJIT-G/omnitoolaz-ai-life-planner.git
git push -u origin main
